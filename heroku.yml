
build:
  docker:
    # web server container
    web: server/Dockerfile
    # math worker container
    worker: math/Dockerfile
    client-participation: client-participation/Dockerfile
    client-admin: client-admin/Dockerfile
    client-report: client-report/Dockerfile
    release: ./Dockerfile

release:
  image: release
  command:
    - ./deploy-static-assets.clj


