
build:
  docker:
    # web server container
    web: server/Dockerfile
    # math worker container
    worker: math/Dockerfile
    compdem/polis-client-participation: client-participation/Dockerfile
    compdem/polis-client-admin: client-admin/Dockerfile
    compdem/polis-client-report: client-report/Dockerfile
    release: ./Dockerfile

release:
  image: release
  command:
    - ./deploy-static-assets.clj


