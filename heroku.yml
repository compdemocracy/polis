
build:
  docker:
    # web server container
    web: server/Dockerfile
    # math worker container
    worker: math/Dockerfile
    # build and release static assets (js, index.html, etc)
    release: Dockerfile

release:
  image: release
  command:
    - ./deploy-static-assets.clj --bucket $STATIC_ASSET_DEPLOY_BUCKET


