
build:
  docker:
    # web server container
    web: server/Dockerfile
    # math worker container
    worker: math/Dockerfile
    compodem/polis-client-participation: client-participation/Dockerfile
    compodem/polis-client-admin: client-admin/Dockerfile
    compodem/polis-client-report: client-report/Dockerfile
    release: ./Dockerfile

release:
  image: release
  command:
    - ./deploy-static-assets.clj


