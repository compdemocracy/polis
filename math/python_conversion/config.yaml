## Pol.is Math Configuration
math_env: dev

# Data storage
data_dir: "./data"

# Database settings - disabled for testing
database:
  enabled: false

# Server settings
server:
  enabled: true
  host: "127.0.0.1"
  port: 8000
  log_level: "info"  # using lowercase to fix KeyError

# Polling settings - disabled for testing
poller:
  enabled: false

# Compute settings
compute:
  n_clusters: 3
  pca_iters: 10

# Logging settings
logging:
  level: "INFO"