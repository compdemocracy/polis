# This directory ships as the `mathsrc` additional build context of
# delphi/Dockerfile — keep caches, a temporarily-restored Clojure oracle
# tree (see README.md), and build litter out of the context.
__pycache__/
*.py[cod]
.pytest_cache/
dist/
build/
*.egg-info/
# Restored-oracle entries (present only during a certify re-replay):
src/
test/
dev/
resources/
bin/
misc/
doc/
data/
tmp/
deps.edn
