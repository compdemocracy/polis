{{> header}}


<section class="container" >
  <div class="row">
    <div class="col-sm-12 col-md-6">
      <input id="newContextInput"> <button id="newContextBtn">create polis topic</button>
      {{#unless contexts}}
      <p class="col-md-6 col-md-offset-3 collectionLoading HeadingC" style="text-align:center;">Loading...</p>
      {{/unless}}
      {{#each contexts}}
        <div><a href="/s/{{name}}">/s/{{name}}</a></div>
      {{/each}}

    </div>
  </div>
</section>