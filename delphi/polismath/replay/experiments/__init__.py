"""Committed, reproducible experiment suites for replay schedule inference.

Currently one suite: cut-cadence localization (:mod:`.cadence`, CLI in
:mod:`.run_cadence`). Results are committed next to the code
(``results_cadence.json`` + ``RESULTS.md``) with exact reproduce commands.
"""
