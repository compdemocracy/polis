{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://polis.local/schemas/certify_datasets-v1.json",
  "title": "Certification fixture selection config (P-022 A)",
  "description": "Declarative, committed-before-inspection selection rules that map coverage ROLES to production conversations by numeric metrics only. Never contains zids, report ids, topics or text. Validated by polismath.replay.fixture_config.validate_config, which implements the Draft-07 subset used here plus the semantic rules the schema cannot express (see SEMANTIC RULES in that module).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "config_version",
    "metrics",
    "roles",
    "workloads",
    "generated"
  ],
  "properties": {
    "schema_version": {"const": "certify-datasets/1"},
    "config_version": {"type": "string", "minLength": 1},
    "notes": {"type": "string"},
    "public_fixtures": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["slug", "role"],
        "properties": {
          "slug": {"type": "string", "minLength": 1},
          "role": {"type": "string", "minLength": 1},
          "notes": {"type": "string"}
        }
      }
    },
    "coverage_role_map": {
      "type": "object",
      "description": "PUBLIC mapping of the retired Julien coverage role name -> the new neutral role name. Contains no identities.",
      "additionalProperties": {"type": "string"}
    },
    "metrics": {
      "type": "object",
      "description": "Committed metric definitions. Each entry states the numerator/denominator in prose; the executable definition lives in polismath.replay.fixture_survey.METRIC_DEFINITIONS and the two are cross-checked by tests.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["definition"],
        "properties": {
          "definition": {"type": "string", "minLength": 1},
          "denominator": {"type": "string"},
          "null_when": {"type": "string"}
        }
      }
    },
    "representative_selection": {
      "type": "object",
      "description": "Optional snapshot selection/report stage only. Does not add payloads, change existing recipes or admit representative replay. A reviewed config/version fixes the seed before candidate output is inspected; absence preserves existing extraction behavior.",
      "additionalProperties": false,
      "required": ["algorithm", "target", "seed"],
      "properties": {
        "algorithm": {"type": "string", "const": "representative-logpv/1"},
        "target": {"type": "integer", "const": 20},
        "seed": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
      }
    },
    "served_math": {
      "type": "object",
      "description": "OPTIONAL (P-052 section 4.5). When present with capture=true, each extracted conversation additionally captures what the Clojure engine actually SERVED: the math_main row per math_env (blob verbatim, last_vote_timestamp, math_tick, caching_tick, modified) and the math_ticks row. This is an EXTRACTION change only - two additional SELECTs against tables that already exist, no migration, no new column, no trigger, no write. The block is ABSENT from the shipped config, which is what keeps the capture off by default and keeps certify_datasets.json byte-identical (and therefore every published manifest's commits.config_sha256 unchanged). Turning it on is a reviewed edit that mints a new config version.",
      "additionalProperties": false,
      "required": ["capture"],
      "properties": {
        "capture": {"type": "boolean"},
        "math_envs": {
          "type": "array",
          "description": "Restrict the capture to these math_env values. Omit to capture every math_env the conversation has a row for. Only meaningful with capture=true.",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        },
        "notes": {"type": "string"}
      }
    },
    "roles": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/definitions/role"}
    },
    "workloads": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "kind", "references", "description"],
        "properties": {
          "id": {"type": "string", "minLength": 1},
          "kind": {"enum": ["concurrent", "churn"]},
          "references": {"type": "array", "items": {"type": "string"}},
          "description": {"type": "string"},
          "concurrency": {"type": "integer", "minimum": 1},
          "small_job_stream": {"type": "array", "items": {"type": "string"}},
          "cache_cap_plus_one_cohort": {
            "type": "object",
            "additionalProperties": false,
            "required": ["generator_case", "touch_after_eviction"],
            "properties": {
              "generator_case": {"type": "string"},
              "touch_after_eviction": {"type": "string"}
            }
          }
        }
      }
    },
    "generated": {
      "type": "object",
      "additionalProperties": false,
      "required": ["generator_id", "generator_version", "seed", "cases"],
      "properties": {
        "generator_id": {"type": "string", "minLength": 1},
        "generator_version": {"type": "string", "minLength": 1},
        "seed": {"type": "integer", "minimum": 0},
        "cases": {
          "type": "array",
          "minItems": 1,
          "items": {"$ref": "#/definitions/generated_case"}
        }
      }
    }
  },
  "definitions": {
    "role": {
      "type": "object",
      "additionalProperties": false,
      "required": ["role", "slug", "group", "rank", "predicates", "order_by", "on_missing", "exercise"],
      "properties": {
        "role": {"type": "string", "minLength": 1},
        "slug": {"type": "string", "pattern": "^pc-v1-[a-z0-9-]+$"},
        "group": {
          "enum": ["replacement", "stress"],
          "description": "replacement = one of the thirteen unique-conversation roles; stress = added role that MAY reuse a conversation already taken (overlap is disclosed in the manifest)."
        },
        "rank": {
          "type": "integer",
          "minimum": 1,
          "description": "1-based rank into the ordered candidate list after predicates (and, for a group=replacement role, after excluding conversations already reserved by earlier roles)."
        },
        "selection_group": {
          "type": "string",
          "minLength": 1,
          "description": "Roles sharing this name are resolved together against ONE candidate list, so their ranks are positions in the same ordering rather than successive picks from a shrinking list. They must share identical predicates and order_by, and must have distinct ranks. This is what the spec's 'among remaining V>=50,000 conversations, ... select ranks 1,2,4,8,16' means."
        },
        "predicates": {
          "type": "array",
          "items": {"$ref": "#/definitions/predicate"}
        },
        "order_by": {
          "type": "array",
          "minItems": 1,
          "items": {"$ref": "#/definitions/sort_key"}
        },
        "on_missing": {
          "enum": ["fail", "fail_with_synthetic_replacement_offer"],
          "description": "A missing role stops bundle construction. The replacement-offer mode names the deterministic public fixture an approver may explicitly accept instead. Neither mode silently downgrades."
        },
        "synthetic_replacement": {"type": "string"},
        "exercise": {"type": "string", "minLength": 1},
        "notes": {"type": "string"}
      }
    },
    "predicate": {
      "type": "object",
      "additionalProperties": false,
      "required": ["metric", "op", "value"],
      "properties": {
        "metric": {"type": "string", "minLength": 1},
        "op": {"enum": ["ge", "gt", "le", "lt", "eq", "ne"]},
        "value": {"type": "number"}
      }
    },
    "sort_key": {
      "type": "object",
      "additionalProperties": false,
      "required": ["metric", "direction"],
      "properties": {
        "metric": {"type": "string", "minLength": 1},
        "direction": {"enum": ["asc", "desc"]}
      }
    },
    "generated_case": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "boundary", "participants", "comments", "shape"],
      "properties": {
        "id": {"type": "string", "pattern": "^gen-v1-[a-z0-9-]+$"},
        "boundary": {"type": "string", "minLength": 1},
        "participants": {"type": "integer", "minimum": 0},
        "comments": {"type": "integer", "minimum": 0},
        "shape": {
          "enum": [
            "dense-block", "sparse-strip", "all-pass", "constant-column",
            "no-eligible-comments", "repeated-timestamps", "out-of-order-commits",
            "dormant", "empty", "lru-cohort"
          ]
        },
        "cohort_size": {
          "type": "integer",
          "minimum": 1,
          "description": "lru-cohort only: how many sibling public fixture zids the case materialises (set to cache_cap + 1)."
        },
        "votes_per_participant": {"type": "integer", "minimum": 0},
        "mod_out_all": {"type": "boolean"},
        "dormant_age_days": {"type": "integer", "minimum": 0},
        "heavy": {
          "type": "boolean",
          "description": "Scale control that is declared but NOT materialised unless --include-heavy is passed; its byte size makes it unsuitable for the default bundle."
        },
        "notes": {"type": "string"}
      }
    }
  }
}
