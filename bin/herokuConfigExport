#!/bin/sh

# usage (unsafe!)
#  eval `./bin/herokuConfigExport`

heroku config --app polisapp | grep ": " | sed "s/[:=] */='/" | sed "s/$/'/" | sed "s/^/export /"

# Set developer enviroment ("Beta") vars here:
echo "export APPLICATION_NAME=PolisWebServerBeta"

